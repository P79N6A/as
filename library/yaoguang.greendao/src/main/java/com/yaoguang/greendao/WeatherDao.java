package com.yaoguang.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.yaoguang.greendao.entity.Weather;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WEATHER".
*/
public class WeatherDao extends AbstractDao<Weather, Long> {

    public static final String TABLENAME = "WEATHER";

    /**
     * Properties of entity Weather.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ReportTime = new Property(1, String.class, "reportTime", false, "REPORT_TIME");
        public final static Property Weather = new Property(2, String.class, "weather", false, "WEATHER");
        public final static Property Temperature = new Property(3, String.class, "temperature", false, "TEMPERATURE");
        public final static Property WindDirection = new Property(4, String.class, "windDirection", false, "WIND_DIRECTION");
        public final static Property WindPower = new Property(5, String.class, "windPower", false, "WIND_POWER");
        public final static Property Humidity = new Property(6, String.class, "humidity", false, "HUMIDITY");
        public final static Property Error = new Property(7, String.class, "error", false, "ERROR");
    }


    public WeatherDao(DaoConfig config) {
        super(config);
    }
    
    public WeatherDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WEATHER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"REPORT_TIME\" TEXT," + // 1: reportTime
                "\"WEATHER\" TEXT," + // 2: weather
                "\"TEMPERATURE\" TEXT," + // 3: temperature
                "\"WIND_DIRECTION\" TEXT," + // 4: windDirection
                "\"WIND_POWER\" TEXT," + // 5: windPower
                "\"HUMIDITY\" TEXT," + // 6: humidity
                "\"ERROR\" TEXT);"); // 7: error
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WEATHER\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Weather entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String reportTime = entity.getReportTime();
        if (reportTime != null) {
            stmt.bindString(2, reportTime);
        }
 
        String weather = entity.getWeather();
        if (weather != null) {
            stmt.bindString(3, weather);
        }
 
        String temperature = entity.getTemperature();
        if (temperature != null) {
            stmt.bindString(4, temperature);
        }
 
        String windDirection = entity.getWindDirection();
        if (windDirection != null) {
            stmt.bindString(5, windDirection);
        }
 
        String windPower = entity.getWindPower();
        if (windPower != null) {
            stmt.bindString(6, windPower);
        }
 
        String humidity = entity.getHumidity();
        if (humidity != null) {
            stmt.bindString(7, humidity);
        }
 
        String error = entity.getError();
        if (error != null) {
            stmt.bindString(8, error);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Weather entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String reportTime = entity.getReportTime();
        if (reportTime != null) {
            stmt.bindString(2, reportTime);
        }
 
        String weather = entity.getWeather();
        if (weather != null) {
            stmt.bindString(3, weather);
        }
 
        String temperature = entity.getTemperature();
        if (temperature != null) {
            stmt.bindString(4, temperature);
        }
 
        String windDirection = entity.getWindDirection();
        if (windDirection != null) {
            stmt.bindString(5, windDirection);
        }
 
        String windPower = entity.getWindPower();
        if (windPower != null) {
            stmt.bindString(6, windPower);
        }
 
        String humidity = entity.getHumidity();
        if (humidity != null) {
            stmt.bindString(7, humidity);
        }
 
        String error = entity.getError();
        if (error != null) {
            stmt.bindString(8, error);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Weather readEntity(Cursor cursor, int offset) {
        Weather entity = new Weather( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // reportTime
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // weather
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // temperature
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // windDirection
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // windPower
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // humidity
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7) // error
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Weather entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setReportTime(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setWeather(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setTemperature(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setWindDirection(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setWindPower(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setHumidity(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setError(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Weather entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Weather entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Weather entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
